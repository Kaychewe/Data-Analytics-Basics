#include <stdio.h>
#include <stdlib.h>

/*INSERTION SORT ALGORITHM*/

insertion_sort(item s[], int n)
{
    int i, j;
    for (i=1; i<n; i++){
        j = i;
        while(j>0) && (s[j]{swap(& s[j], &s [j-1]);
            j = j -1;
        })
    }

}